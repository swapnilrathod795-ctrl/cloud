name: CI
on: push

jobs:
   test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Running tests"
 deploy:
   needs: test
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v4
     - run: echo "deploying to production"






